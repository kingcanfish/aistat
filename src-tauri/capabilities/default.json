{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions the status panel needs: listening for background refresh events.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
